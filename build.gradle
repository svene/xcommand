project.ext {
	DEP_SPRING_FW_BOM = 'org.springframework:spring-framework-bom:7.0.6'
}
subprojects {
	tasks.withType(Test) {
		testLogging {
			events "passed", "skipped", "failed"
			exceptionFormat = "short"
		}
	}

}
